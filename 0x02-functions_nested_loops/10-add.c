#include "main.h"
/**
 * add - adds two integers and returnsthe result
 *
 * @X: number being added
 *
 * @y: number being added
 *
 * Return: Always 0.
 *
 */
int add(int X, int y)
{
	return (X + y);
}
